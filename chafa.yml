- module-name: chafa
  dlls:
    - from_filenames:
        relative_path: libs
        prefixes:
          - lib
        suffixes:
          - dll
      when: win32
    - from_filenames:
        relative_path: libs
        prefixes:
          - lib
        suffixes:
          - so
      when: linux